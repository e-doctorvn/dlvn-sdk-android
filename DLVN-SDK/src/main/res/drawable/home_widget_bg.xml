<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Outer border with #F1F1F1 color -->
    <solid android:color="#F1F1F1" />
    <!-- Outer border radius -->
    <corners android:radius="8dp" />
    <!-- Inner border -->
    <stroke
        android:width="1dp"
        android:color="#E2E2E2" />
</shape>